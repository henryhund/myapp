<%= simple_form_for(@calendar) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :info, label: "Info about your Availability" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
